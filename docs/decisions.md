# יומן החלטות — החווה של ריקי

מסמך זה מתעד החלטות ארכיטקטוניות ואת הנימוק להן.
נכתב תוך כדי העבודה. מהווה בסיס למפרט טכני לשכפול.

---

## 01 Sanity כ-backend התוכן

**תאריך:** 2026-08-18
**משימה:** החלטת ארכיטקטורה (לפני משימה 0)

**מה הוחלט:**
ניהול התוכן ייבנה על Sanity, ולא על פתרון עצמאי.

**למה:**
טיפול בתמונות מובנה (העלאה, crop, hotspot, CDN) — הרכיב הכבד ביותר לבנייה עצמית. אין צורך לבנות הרשאות, גרסאות ו-audit log מאפס. עריכה לא דורשת deploy מחדש, ויש היסטוריה שמאפשרת לשחזר טקסט שנמחק בטעות.

**מה נדחה:**
JSON בריפו — כל שינוי דורש commit, לא מתאים למשתמשת לא טכנית. בניית DB ופאנל admin מאפס — חודש עבודה על משהו שקיים מהמדף.

**מי החליט:** כפיר

**מקור:** ההנחיה בשיחה, סעיף "החלטת ארכיטקטורה"

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 02 סטודיו מוטמע באפליקציית Next, ולא סטודיו מתארח

**תאריך:** 2026-08-18
**משימה:** 1

**מה הוחלט:**
הסטודיו רץ בתוך אותו פרויקט Next.js בנתיב `/studio`, ולא כאפליקציה נפרדת שמתארחת אצל Sanity.

**למה:**
הנימוק לא תועד בזמן ההחלטה.

**מה נדחה:**
לא נשקלה אלטרנטיבה.

**מי החליט:** כפיר

**מקור:** ההנחיה במשימה 1, סעיף 2

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 03 ייבוא קונפיג הסטודיו מאחורי גבול `'use client'`

**תאריך:** 2026-08-18
**משימה:** 1

**מה הוחלט:**
`sanity.config.ts` מיובא מקומפוננטת לקוח ייעודית ולא ישירות מ-`page.tsx`.

**למה:**
חבילת `sanity` מייבאת את ה-default של `swr`. כשהקונפיג נמצא בגרף ה-Server Component חל תנאי הייצוא `react-server`, ובבילד הזה של `swr` אין default export — הראוט החזיר 500.

**מה נדחה:**
עקיפה ב-`next.config.ts` (alias או `serverExternalPackages`) — נדחתה כי היא מסתירה את הסיבה ומשפיעה על כל הפרויקט, במקום לתחום את הבעיה למקום שבו היא נוצרת.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** קומיט `fd56680`, הקובץ `app/studio/[[...tool]]/Studio.tsx`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 04 דריסות CSS מתוחמות לסטודיו, במקום layout שורש נפרד

**תאריך:** 2026-08-18
**משימה:** 1

**מה הוחלט:**
ה-layout של `/studio` דורס כללים גלובליים ומסתיר ווידג'טים של האתר, במקום להוציא את האתר ל-route group עם layout שורש משלו.

**למה:**
ה-layout הראשי מזריק לכל ראוט את טבעת הפוקוס של האתר (שמבוססת על צבע שאסור לפי אילוץ המותג), ומרנדר את ווידג'ט הנגישות ובאנר העוגיות כילדים ישירים של `body`. שניהם לא שייכים למסך ניהול.

**מה נדחה:**
העברת כל עמודי האתר ל-route group בשם `(site)` — נדחתה כי היא נוגעת בעשרות קבצים שאינם קשורים למשימה.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** קומיט `fd56680`, הקובץ `app/studio/[[...tool]]/layout.tsx`

**האם ניתן לשכפול ללקוח אחר:** כן בשינוי — תלוי במה ה-layout הראשי של הלקוח מזריק

---

## 05 חריג ב-`.gitignore` לקובץ הדוגמה של משתני הסביבה

**תאריך:** 2026-08-18
**משימה:** 1

**מה הוחלט:**
נוספה שורת שלילה ב-`.gitignore` כדי שקובץ הדוגמה ייכנס לגיט, בעוד קבצי הסביבה האמיתיים נשארים חסומים.

**למה:**
הכלל הקיים `.env*` תפס גם את קובץ הדוגמה, שהוא תוצר מחייב של המשימה. בלי החריג אי אפשר היה למסור אותו.

**מה נדחה:**
לתת לקובץ שם שלא מתחיל בנקודה כדי לעקוף את הכלל — נדחתה כי היא שוברת את המוסכמה המקובלת ומקשה על מפתח חדש למצוא את הקובץ.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** קומיט `fd56680`, הקובץ `.gitignore`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 06 הרחבת הסכמות הקיימות במקום יצירת סכמות חדשות

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
הסכמות הקיימות בריפו הורחבו במקומן, ולא נוצרו לצידן שלוש סכמות חדשות כפי שהמפרט ביקש.

**למה:**
שתיים משלוש הסכמות במפרט חפפו כמעט לחלוטין לסכמות שכבר היו בריפו. יצירתן במקביל הייתה משאירה את העורכת עם שני סוגי "מארז" בלי דרך לדעת איזה מהם האתר קורא.

**מה נדחה:**
יצירת שלוש הסכמות החדשות לצד הקיימות — נדחתה בגלל הכפילות. מחיקת הקיימות ובנייה מחדש — נדחתה כי היא שוברת את שכבת הקריאה הקיימת בקוד.

**מי החליט:** כפיר, באישור המלצה של Claude Code

**מקור:** קומיט `cdec538`, וההנחיה "קדימה — הרחב את הסכמות הקיימות. אל תיצור סכמות חדשות שכפולות."

**האם ניתן לשכפול ללקוח אחר:** כן בשינוי — רלוונטי רק כשקיימות סכמות קודמות בריפו

---

## 07 `mediaBlock` כסכמה חדשה יחידה

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
מתוך שלוש הסכמות שהמפרט ביקש, רק `mediaBlock` נוצרה כסכמה חדשה.

**למה:**
נבדקו כל חמש הסכמות הקיימות ולא נמצא בהן שדה או ישות שמתאימים לבאנרים ולתמונות אתר. זו הסכמה היחידה שאינה כפילות.

**מה נדחה:**
הרחבה של אחת הסכמות הקיימות כדי לארח בה גם תמונות אתר — נדחתה כי מדובר בישויות שונות, והערבוב היה מבלבל את העורכת.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** קומיט `cdec538`, הקובץ `sanity/schemas/mediaBlock.ts`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 08 צבע הבאנר — טיפוס טקסט עם רשימה סגורה

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
שדה צבע הבאנר נשאר מטיפוס טקסט, אך קיבל רשימת ערכים סגורה מפלטת המותג בלבד.

**למה:**
הכלל שנקבע למשימה אסר לשנות טיפוס של שדה קיים. במקביל, ההגדרה הקודמת של השדה הציעה לעורכת במפורש ערך שאסור לפי אילוץ המותג — כלומר הסטודיו עצמו הכווין אותה לצבע פסול. רשימה סגורה פותרת את שתי המגבלות יחד.

**מה נדחה:**
שינוי הטיפוס לטיפוס צבע ייעודי, ומחיקת השדה — שניהם נדחו כי הכלל אסר לשנות טיפוס או למחוק שדה קיים.

**מי החליט:** כפיר

**מקור:** ההנחיה "banner.color — שנה description. בנוסף: הוסף options.list עם ערכים סגורים מפלטת המותג בלבד", והקובץ `sanity/schemas/siteSettings.ts`

**האם ניתן לשכפול ללקוח אחר:** כן בשינוי — הפלטה עצמה ספציפית ללקוח

---

## 09 שדות המחיר נשארים טקסט

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
שדה המחיר הקיים נשאר טקסט, ושדה המחיר החדש לכמות גדולה נוצר גם הוא כטקסט.

**למה:**
הכלל למשימה אסר לשנות טיפוס של שדה קיים, והשדה הקיים הוגדר כטקסט מלכתחילה כדי לאפשר ניסוחים כמו "לפי הצעת מחיר". השדה החדש הוגדר באותו טיפוס כדי שלא יהיו שני מחירים בהתנהגות שונה.

**מה נדחה:**
הגדרת שדות המחיר כמספרים, כפי שהמפרט רמז — נדחתה בגלל הכלל, ובגלל שניסוחים שאינם מספר הם דרישה עסקית אמיתית.

**מי החליט:** כפיר

**מקור:** ההנחיה "price — נשאר string. bulkPrice גם string. אשר.", והקובץ `sanity/schemas/package.ts`

**האם ניתן לשכפול ללקוח אחר:** כן בשינוי

---

## 10 קטגוריה כרשימה סגורה, בנפרד מהתגיות

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
נוסף שדה קטגוריה עם רשימת ערכים סגורה, והשדה הקיים לתגיות חופשיות נשאר כפי שהיה. רשימות הערכים רוכזו בקובץ קבועים אחד.

**למה:**
העורכת אינה טכנית. הקלדה חופשית של שם עונה מייצרת גרסאות שונות לאותו ערך, והאתר מפסיק לסנן נכון. ריכוז הרשימה במקום אחד הופך הוספת עונה לשינוי של שורה אחת.

**מה נדחה:**
שימוש חוזר בשדה התגיות החופשי לצורך קטגוריה — נדחה בגלל סכנת חוסר העקביות.

**מי החליט:** כפיר, באישור המלצה של Claude Code

**מקור:** ההנחיה "אפשרות (ב) — category חדש עם רשימה סגורה, tags נשאר חופשי", והקובץ `sanity/schemas/constants.ts`

**האם ניתן לשכפול ללקוח אחר:** כן — ערכי הרשימה משתנים בין לקוחות

---

## 11 ולידציה כאזהרה בלבד, לא כחסימה

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
כל מגבלות האורך שנוספו הוגדרו כאזהרה. אף מגבלה חדשה אינה חוסמת שמירה.

**למה:**
עדיף שהעורכת תשמור תוכן ארוך מדי ותראה הערה, מאשר שתיתקע ולא תוכל לשמור כלל. חסימה אצל משתמשת לא טכנית מובילה לנטישת הממשק.

**מה נדחה:**
ולידציה חוסמת — נדחתה מהסיבה שלמעלה. חובות ה-`required` שכבר היו בקוד לא שונו.

**מי החליט:** כפיר

**מקור:** ההנחיה "השתמש ב-warning ולא ב-error, כדי לא לחסום עריכה"

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 12 אכיפת singleton להגדרות האתר בשתי שכבות

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
מסמך הגדרות האתר נאכף כמסמך יחיד: מזהה מסמך קבוע ופריט יחיד בסרגל, ובנוסף סינון מכל תפריטי "צור חדש".

**למה:**
מבנה סרגל בלבד אינו מספיק — כפתור ההוספה הגלובלי עדיין מאפשר ליצור עותק שני, והאתר היה קורא רק אחד מהם. שתי השכבות יחד סוגרות את הפרצה.

**מה נדחה:**
הסתרה באמצעות מבנה הסרגל בלבד — נדחתה כי אינה מונעת יצירה בפועל.

**מי החליט:** כפיר

**מקור:** ההנחיה "כלול את אכיפת ה-singleton של siteSettings... הסתר את כפתור 'צור חדש' עבורו", והקובץ `sanity.config.ts`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 13 סקריפט ההזרמה מוגן בהרצה יבשה כברירת מחדל

**תאריך:** 2026-08-18
**משימה:** 3

**מה הוחלט:**
הסקריפט רץ במצב יבש כברירת מחדל ואינו כותב דבר, אלא אם הועבר לו במפורש דגל כתיבה.

**למה:**
הרצה בשוגג של סקריפט הזרמה על דאטהסט חי היא נזק שקשה לתקן. ברירת מחדל שאינה כותבת מאפשרת לראות בדיוק מה יקרה לפני שמשהו קורה. בנוסף, כל מסמך נכתב עם מזהה קבוע ובפעולה שאינה דורסת, כך שהרצה חוזרת לא מוחקת עריכות של העורכת.

**מה נדחה:**
סקריפט שכותב מיד בהרצה — נדחה בגלל סיכון הדריסה. כתיבה דורסת — נדחתה כי היא הופכת כל הרצה חוזרת למחיקת עבודה.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** הקובץ `scripts/seed-sanity.ts`, הדגל `--commit`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 14 סרטונים עם קישור זמני לא הוזרמו

**תאריך:** 2026-08-18
**משימה:** 3

**מה הוחלט:**
שני הסרטונים שבקוד לא הועברו לניהול התוכן.

**למה:**
הקישור שלהם בקוד הוא ערך זמני ולא כתובת אמיתית, והשדה בסכמה מוגדר כחובה. הזרמה הייתה יוצרת שני מסמכים שנראים כתוכן תקין אך מובילים לשום מקום, והעורכת לא הייתה יודעת שהם ריקים.

**מה נדחה:**
הזרמת הערך הזמני כפי שהוא — נדחתה כי היא מזריקה נתון מזויף למערכת. המצאת קישור אמיתי — אסורה לפי כלל העבודה.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** `lib/config.ts`, מערך הסרטונים; והפלט של הסקריפט תחת "תוכן שלא הועבר"

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 15 הקטגוריה לא נגזרה מהתגיות הקיימות

**תאריך:** 2026-08-18
**משימה:** 3

**מה הוחלט:**
שדה הקטגוריה נשאר ריק בכל המארזים שהוזרמו, במקום להיגזר אוטומטית מהתגיות שכבר קיימות בקוד.

**למה:**
התגיות בקוד אינן מיפוי חד-ערכי לרשימה הסגורה שנקבעה במשימה 2 — למארז אחד יש כמה תגיות, וחלקן אינן מתאימות לאף ערך ברשימה. גזירה אוטומטית הייתה בחירה שלי ולא נתון שקיים בקוד.

**מה נדחה:**
גזירת הקטגוריה מהתגית הראשונה — נדחתה כי היא ניחוש שנראה כמו נתון.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** הכלל "אל תמציא" במשימה 3; הפלט של הסקריפט תחת "שדות לא מולאו"

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 16 רוב תוכן האתר אינו מכוסה על ידי הסכמות

**תאריך:** 2026-08-18
**משימה:** 3

**מה הוחלט:**
ארבעה-עשר גושי תוכן שקיימים בקוד לא הוזרמו, מפני שאין להם שדה באף סכמה. הם תועדו ולא נמחקו ולא נדחסו לשדה קיים.

**למה:**
המפרט המקורי הגדיר שלוש סכמות בלבד. שאלות ותשובות, המלצות, יתרונות, פסקאות הסיפור, הפרטים המשפטיים ומחירון הצ'אטבוט נשארים מחוץ להישג ידה של העורכת. זו מגבלת היקף אמיתית שנחשפה רק בשלב ההזרמה.

**מה נדחה:**
הרחבת הסכמות ביוזמתי כדי לכסות את הפער — נדחתה כי היא חורגת מהיקף המשימה ומחייבת החלטה של בעל המוצר. דחיסת התוכן לשדות טקסט קיימים — נדחתה כי היא הופכת תוכן מובנה לטקסט חופשי.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** הפלט של `scripts/seed-sanity.ts` תחת "תוכן שלא הועבר"

**האם ניתן לשכפול ללקוח אחר:** כן בשינוי — היקף הפער תלוי בכמות התוכן אצל הלקוח

---

## 17 התוכן הוזרם דרך ערוץ חלופי, בלי תמונות

**תאריך:** 2026-08-18
**משימה:** 3

**מה הוחלט:**
שלושים מסמכי טקסט הוזרמו ופורסמו דרך ממשק הניהול של Sanity במקום דרך הסקריפט, וארבעה בלוקי מדיה וכל התמונות לא הוזרמו כלל.

**למה:**
מדיניות הרשת של סביבת הריצה חוסמת גם את ממשק ה-API של Sanity וגם את שירות התמונות שבו יושבות תמונות האתר. הסקריפט עצמו תקין, אך לא יכול לרוץ מכאן. המטענים נוצרו מהסקריפט עצמו כדי שלא תהיה סטייה בין מה שאושר לבין מה שנכתב בפועל.

**מה נדחה:**
המתנה עד שיתאפשר להריץ את הסקריפט — נדחתה כי היא חוסמת את משימה 4 ללא צורך. הזרמת בלוקי מדיה בלי תמונה — נדחתה כי התמונה היא שדה חובה שם, והמסמך היה נוצר פגום.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** תשובת הפרוקסי 403 עבור שני היעדים; הדגל `--json` ב-`scripts/seed-sanity.ts`

**האם ניתן לשכפול ללקוח אחר:** לא — מגבלת סביבה, לא החלטת מוצר. בסביבה עם גישת רשת מריצים את הסקריפט כרגיל והתמונות עוברות איתו.

---

## 18 יישור שמות משתני הסביבה בין האתר לסטודיו

**תאריך:** 2026-08-18
**משימה:** 4

**מה הוחלט:**
שכבת הקריאה של האתר עברה לקרוא את מזהי הפרויקט והדאטהסט מאותם שמות שהסטודיו משתמש בהם, וטוקן הקריאה יושר לשם שמתועד בקובץ הדוגמה. סוגר את פריט א' ב"לבירור מול כפיר".

**למה:**
עד כה האתר קרא שמות ללא תחילית ציבורית והסטודיו קרא שמות עם תחילית, כך ששניהם יכלו להצביע על מקורות שונים בלי שאיש ישים לב. סטודיו מוטמע רץ בדפדפן ולכן מחויב לתחילית הציבורית; המזהים אינם סודות, והטוקן נשאר בצד השרת בלבד.

**מה נדחה:**
יישור לכיוון ההפוך, כלומר הסרת התחילית — נדחתה כי הסטודיו לא היה עולה בלעדיה.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** `lib/sanity.ts`; פריט א' ב"לבירור מול כפיר" ברשומות הקודמות

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 19 קריאה ישירה מ-Sanity ולא דרך ה-CDN שלו

**תאריך:** 2026-08-18
**משימה:** 4

**מה הוחלט:**
הלקוח מוגדר לא לעבור דרך ה-CDN של Sanity.

**למה:**
שכבת המטמון של האתר היא ISR של דקה בתוספת webhook לרענון מיידי. מטמון נוסף ובלתי נשלט מעל זה היה יכול לגרום לכך שפרסום ייקח יותר מהדקה שהובטחה, בלי שנוכל לדעת מתי.

**מה נדחה:**
השארת ה-CDN דלוק — נדחתה בגלל אי-הוודאות בזמן ההתעדכנות. המחיר: יותר קריאות ל-API, אך ISR מגביל אותן לקריאה אחת לדקה לכל היותר.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** `lib/sanity.ts`, ההגדרה `useCdn`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 20 ראוט הרענון סגור כברירת מחדל ומרענן נתיב אחד

**תאריך:** 2026-08-18
**משימה:** 4

**מה הוחלט:**
ראוט הרענון דורש סוד משותף בכותרת, מחזיר שגיאה כשהסוד אינו מוגדר כלל, ומרענן את עמוד הבית בלבד.

**למה:**
בלי בדיקת סוד כל אחד ברשת יכול היה להפעיל רענון חוזר ולהעמיס על האתר ועל Sanity. סביבה שבה הסוד לא הוגדר היא סביבה שבה הראוט פתוח, ולכן הוא נסגר מעצמו במקום לעבוד ללא הגנה. ההשוואה נעשית בזמן קבוע כדי לא לדלוף מידע על הסוד.

**מה נדחה:**
רענון לפי סוג המסמך שהשתנה — נדחה כי כל התוכן הקיים מוזן לעמוד הבית בלבד, וגרנולריות מדומה הייתה מטעה. ראוט ללא הגנה — נדחה מטעמי אבטחה.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** `app/api/revalidate/route.ts`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 21 שרשרת נפילה לתמונות מארז, והסרת צבע אסור מברירות המחדל

**תאריך:** 2026-08-18
**משימה:** 4

**מה הוחלט:**
מארז ללא תמונה בניהול התוכן מציג את התמונה הסטטית הקיימת באתר לפי המזהה שלו, ורק אם גם היא חסרה מוצגת תמונת ברירת מחדל בצבעי המותג.

**למה:**
ההזרמה הכניסה שלושה-עשר מארזים בלי תמונה. ברירת המחדל שהייתה בקוד צבועה בענבר, ולכן חיבור האתר לניהול התוכן היה מחליף תמונות אמיתיות בשלושה-עשר ריבועים בצבע שאסור לפי אילוץ המותג. שרשרת הנפילה שומרת על התמונות שכבר קיימות ומסירה את הצבע האסור.

**מה נדחה:**
השארת ברירת המחדל כפי שהייתה — נדחתה כי היא מפרה את אילוץ המותג. הצגת מארז ללא תמונה כלל — נדחתה כי היא שוברת את פריסת הקטלוג.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** `lib/sanity.ts`, הפונקציה שממפה מארזים

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 22 מזהי Sanity הציבוריים בקובץ התצורה, הסודות בממשק Netlify

**תאריך:** 2026-08-19
**משימה:** 4

**מה הוחלט:**
מזהה הפרויקט ושם הדאטהסט נכתבו לתוך `netlify.toml`. טוקן הקריאה וסוד הרענון נשארים להגדרה ידנית בממשק Netlify ומסומנים כסודות.

**למה:**
שני המזהים נושאים את הקידומת שגורמת ל-Next להטמיע אותם בחבילת הדפדפן בזמן הבנייה, מפני שהסטודיו רץ בדפדפן וזקוק להם. כלומר הם ממילא גלויים לכל מי שפותח את האתר, וניהולם כסודות יוצר עבודה ידנית בלי להגן על דבר. אומת בפועל: המזהה מופיע גם בחבילת השרת וגם בחבילת הלקוח שנבנו. שני הסודות האמיתיים לא עברו לקובץ, כי קובץ התצורה נכנס לגיט.

**מה נדחה:**
הגדרת ארבעת המשתנים בממשק Netlify — נדחתה כי היא מטילה על בעל האתר ארבע פעולות ידניות במקום שתיים, ושתיים מהן חסרות ערך אבטחתי. כתיבת הטוקן לקובץ התצורה — נדחתה כי הקובץ מנוהל בגיט.

**מי החליט:** כפיר, בתשובה "כן תוסיף"

**מקור:** `netlify.toml`, וקובץ ההנחיות של Next תחת `node_modules/next/dist/docs`, המסמך על משתני סביבה

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 23 הרשאות מקור לדומיין הפרודקשן בפרויקט Sanity

**תאריך:** 2026-08-19
**משימה:** 4

**מה הוחלט:**
נוספו שתי הרשאות מקור לפרויקט התוכן — הדומיין הרשמי של האתר, ודומיין האירוח. שתיהן עם הרשאת בקשות מזוהות.

**למה:**
לפני השינוי היו מוגדרות שתי הרשאות בלבד, שתיהן לכתובות פיתוח מקומיות. בלעדיהן מסך הניהול בפרודקשן היה נטען אך נכשל בפנייה לשרת התוכן, ומציג שגיאת רשת במקום מסך התחברות. הדומיין הרשמי נלקח מהגדרת הכתובת הקנונית בקוד האתר, ולא מהשערה.

**מה נדחה:**
הרשאת תו כללי לכל תת-הדומיינים של שירות האירוח — נדחתה כי היא פותחת בקשות מזוהות מכל אתר המתארח באותו שירות. המשמעות היא שמסך הניהול לא ייטען בתצוגות מקדימות של בקשות מיזוג, אלא אם תתווסף הרשאה ייעודית.

**מי החליט:** כפיר, בתשובה "כן תוסיף"

**מקור:** `app/layout.tsx`, הגדרת הכתובת הקנונית; ורשימת הרשאות המקור בפרויקט לפני השינוי ואחריו

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 24 העורכת מקבלת תפקיד מנהלת, והגבלות המחיקה יורדות לרמת הסטודיו

**תאריך:** 2026-08-19
**משימה:** 5

**מה הוחלט:**
בעלת העסק תצורף לפרויקט התוכן בתפקיד מנהלת. הפרדת התפקידים שנקבעה במפרט — עורכת שאינה יכולה למחוק, לצד מנהל — לא תמומש ברמת השרת.

**למה:**
המסלול החינמי של שירות התוכן מציע שני תפקידים בלבד: מנהל, וצופה בקריאה בלבד. תפקיד עורך, תפקיד תורם ותפקידים מותאמים אישית כולם דורשים מסלול בתשלום. תפקיד צופה אינו מאפשר עריכה כלל, ולכן אינו רלוונטי לעורכת התוכן. כלומר לא קיימת דרגת ביניים, ולא רק מניעת המחיקה חסרה — כל ההפרדה חסרה. בעלת העסק אינה מעוניינת בתשלום.

**מה נדחה:**
שדרוג המסלול — נדחה בהחלטת הלקוח. תפקיד צופה — נדחה כי הוא מונע עריכה, שהיא כל מטרת הפרויקט.

**השלכה על משימה 5:**
מה שנותר בר-ביצוע הוא הגנה מפני טעות ולא מפני כוונה: הסתרת פעולת המחיקה מתפריט המסמך, הבלטת המתג הקובע אם פריט מוצג באתר כדרך הנכונה להסיר מוצר, והסתרת סוגי תוכן שאינם לעריכה. מנהלת יכולה לעקוף כל אחת מאלה. הפער תועד כאן במפורש כדי שלא ייחשב לאבטחה. רשת הביטחון בפועל היא היסטוריית הגרסאות שהשירות שומר לכל מסמך.

**מי החליט:** כפיר, בתשובה "טוב אז שתהיה אדמיניסטרטור"

**מקור:** תיעוד השירות, המסמך על תפקידים, בסעיף התפקידים המוגדרים לפי מסלול

**האם ניתן לשכפול ללקוח אחר:** כן, לכל לקוח במסלול חינמי

---

## 25 הסטודיו מציג רק מה שמופיע באתר

**תאריך:** 2026-09-13
**משימה:** 4 (חיבור הפרונט) ו-5 (מעקות בטיחות)

**מה הוחלט:**
סוגי תוכן ושדות שאינם מרונדרים באתר הוסתרו מהסטודיו. הוסתרו סוגי המסמכים סוג ארגז, מוצר לארגז, סרטון ובלוק מדיה, וכן חמישה שדות בתוך מארז: תיאור מלא, מחיר לכמות גדולה, כמות מינימלית, קטגוריה וגלריה. אף מסמך לא נמחק ואף שדה לא הוסר מהסכמה.

**למה:**
בבדיקה לפני מסירה לבעלת העסק נמצא שמתוך שלושים המסמכים שהוזרמו, שישה עשר אינם מגיעים לאתר: הקומפוננטה שבונה ארגז מותאם אינה מחוברת לאף עמוד, ואין אזור שקורא סרטונים או בלוקי מדיה. במקביל, טיפוס המארז שהפרונט מקבל נושא שישה שדות בלבד, כך שחמישה שדות נוספים בטופס אינם משפיעים על דבר. ממשק שמציג טופס שעריכתו אינה משנה דבר גרוע מממשק מצומצם: העורכת תסיק שהמערכת שבורה, ותאבד אמון גם בשדות שכן עובדים.

**מה נדחה:**
חיבור הקומפוננטה לבניית ארגז לעמוד הבית — זו השקה של יכולת שלא אושרה, ולא תיקון חיווט. הוצאתה מהמצב הנוכחי היא החלטה עסקית של בעל האתר, לא החלטה טכנית. גם מחיקת המסמכים שאינם בשימוש נדחתה: הם מייצגים עבודת תוכן אמיתית, והסתרה הפיכה עדיפה על מחיקה שאינה.

**איך מחזירים:**
הסרת השם מרשימת הסוגים המוסתרים בקובץ ההגדרות של הסטודיו, או הסרת שורת ההסתרה מהשדה בסכמה. השינוי נכנס לתוקף בפריסה הבאה, והתוכן שנשמר ממתין במקומו.

**מי החליט:** כפיר, בהנחיה "מסלול ב. בוא נסיים הכל" — כלומר ממשק שכל מה שנראה בו עובד

**מקור:** בדיקה בקוד: `lib/types.ts` (הטיפוס `Package`), היעדר שימוש ב-`BoxBuilder` בכל עמוד, והיעדר קריאה ל-`fetchVideos` ב-`app/page.tsx`

**האם ניתן לשכפול ללקוח אחר:** כן — הכלל הכללי הוא שסכמה לא תציג שדה שהפרונט אינו מרנדר

---

## 26 הבאנר העליון קורא את הצבע ותאריך הסיום שהוגדרו לו

**תאריך:** 2026-09-13
**משימה:** 4 (חיבור הפרונט)

**מה הוחלט:**
הבאנר בראש עמוד הבית מקבל את צבע הרקע ואת תאריך הסיום מהגדרות האתר, במקום רקע קבוע והצגה ללא מגבלת זמן.

**למה:**
שני השדות כבר היו קיימים בסכמה מאז משימה 2, אך העמוד התעלם משניהם. הצבע שנבחר בסטודיו לא השפיע, ובאנר שנשכח היה ממשיך להופיע לנצח. בנוסף, הרקע הקבוע שהיה בקוד הוא גוון חיטה, כלומר צבע ענבר, בניגוד לאילוץ המותג האוסר צהוב וענבר בהתראות. ברירת המחדל כעת היא בורדו, וכל הגוונים המוצעים בסטודיו כהים דיים לטקסט לבן.

**מה נדחה:**
טיפול בתאריך הסיום בצד הלקוח — היה מוסיף הבדל בין מה שהשרת מייצר למה שהדפדפן מציג. הבדיקה נעשית בשרת, ולכן רזולוציית ההיעלמות היא חלון ההתחדשות, עד דקה.

**מי החליט:** נגזר מהסכמה הקיימת ומאילוץ המותג; לא נדרשה הכרעה חדשה

**מקור:** `sanity/schemas/constants.ts` — רשימת הצבעים המוגדרת שם כולה גוונים כהים של המותג

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 27 נקודת בדיקה לחיבור לשירות התוכן

**תאריך:** 2026-09-13
**משימה:** אבחון לפני מסירה

**מה הוחלט:**
נוספה כתובת `‎/api/health/sanity` שמדווחת אם החיבור לשירות התוכן עובד בזמן ריצה, ומחזירה את מספר המארזים ואת כותרת ההירו כפי שהשרת רואה אותם.

**למה:**
כל קריאה לשירות התוכן נופלת חזרה לטקסט שבקוד כשהיא נכשלת. זו התנהגות נכונה כלפי המבקר באתר — הוא רואה אתר תקין במקום שגיאה — אבל היא הופכת כשל לבלתי נראה: אתר מנותק נראה בדיוק כמו אתר מחובר. בפועל זה קרה: הפריסה עברה בירוק, הבנייה הצליחה, ורק ההשוואה הידנית של טקסט הכותרת חשפה שהנתונים אינם מגיעים. בלי נקודת בדיקה, האבחון דורש קריאה בלוגים של הפריסה, וזה לא משהו שבעל האתר יכול לעשות.

הכתובת רצה בכל בקשה ואינה נשמרת בזיכרון מטמון, כי בדיוק ההבדל בין מה שהיה נכון בזמן הבנייה למה שנכון בזמן הריצה הוא מה שצריך לאבחן.

**מה נדחה:**
דיווח הכשל למבקר באתר — היה הופך תקלה טכנית לבעיה שיווקית. גם הסרת ה-fallback נדחתה: היא הייתה הופכת כל תקלה רגעית בשירות התוכן להשבתה של האתר.

**מה לא נחשף:**
הכתובת מחזירה את מזהה הפרויקט ואת שם הדאטהסט, ששניהם ציבוריים ממילא ומוטמעים בחבילת הדפדפן. לגבי הטוקן והסוד היא מדווחת קיום בלבד — כן או לא — ולעולם לא ערך.

**מי החליט:** נגזר מהצורך באבחון; כפיר דיווח שהכותרת באתר אינה מתעדכנת

**מקור:** התנהגות שנצפתה — בנייה ירוקה ב-Netlify לצד תוכן שמגיע מברירת המחדל שבקוד

**האם ניתן לשכפול ללקוח אחר:** כן — כל אתר שנשען על fallback שקט זקוק לדרך לראות שה-fallback פעל

---

## 28 טוקן הקריאה נמחק במקום להיות מוחלף

**תאריך:** 2026-09-13
**משימה:** אבחון לפני מסירה

**מה הוחלט:**
משתנה הסביבה של טוקן הקריאה נמחק מהגדרות האתר בנטליפיי, ולא הוחלף בטוקן תקין. בקוד, ערך ריק או ערך שכולו רווחים נחשב כהיעדר טוקן ואינו נשלח.

**למה:**
נקודת הבדיקה שנוספה ברשומה 27 החזירה `Unauthorized - Session does not match project host` לצד `readTokenPresent: true`. כלומר טוקן היה מוגדר, אך אינו שייך לפרויקט הזה. זהו מצב גרוע יותר מהיעדר טוקן: הדאטהסט ציבורי, ולכן בקשה אנונימית הייתה מצליחה, אבל ברגע שנשלח טוקן השירות בודק אותו ודוחה את הבקשה כולה. הכשל הוסתר מאחורי שרשרת ה-fallback ונראה כמו מערכת תוכן ריקה.

הטוקן גם לא נדרש: כל הקריאות הן קריאות פומביות של תוכן מפורסם, והדאטהסט ציבורי. לכן החלפתו בטוקן תקין הייתה מוסיפה סוד שיש לתחזק ולסובב, בלי שום יכולת שהוא מקנה, ועם אותה נקודת כשל בדיוק במקרה של טעות בהעתקה או שלילה.

**מה נדחה:**
הנפקת טוקן חדש — נדחה כמיותר. ניסיון חוזר ללא טוקן אחרי כשל הרשאה — נדחה כי הוא מסתיר שגיאת תצורה במקום לחשוף אותה, וזו בדיוק התכונה שגרמה לבעיה מלכתחילה.

**השלכה:**
אם הדאטהסט יהפוך לפרטי בעתיד, יידרש טוקן. הקוד ממשיך לקרוא את המשתנה, כך שהגדרתו מחזירה את ההתנהגות בלי שינוי קוד.

**מי החליט:** כפיר קיבל את ההמלצה למחוק ולא להחליף

**מקור:** פלט `‎/api/health/sanity` בפריסת ה-preview, ומצב הדאטהסט `aclMode: public` שאומת מול השירות

**האם ניתן לשכפול ללקוח אחר:** כן — דאטהסט ציבורי לא אמור לקבל טוקן קריאה כלל

---

## 29 האתר לא שולח טוקן קריאה כלל

**תאריך:** 2026-09-13
**משימה:** אבחון לפני מסירה

**מה הוחלט:**
הקוד הפסיק לקרוא את משתנה הסביבה של טוקן הקריאה. הלקוח של שירות התוכן נבנה בלי אימות, וטוקן שמוגדר בסביבה — בכל הקשר ובכל היקף — פשוט אינו בשימוש. נקודת הבדיקה ממשיכה לדווח אם המשתנה קיים, ולצדו שדה שאומר במפורש שאינו בשימוש.

**למה:**
רשומה 28 הסיקה שמחיקת המשתנה בנטליפיי תפתור את הכשל, והשאירה בקוד קריאה של המשתנה למקרה שהדאטהסט ייסגר בעתיד. אחרי המחיקה נקודת הבדיקה החזירה שוב `Unauthorized - Session does not match project host` עם `readTokenPresent: true`. ההנחה שההגדרה בנטליפיי ניתנת לניקוי בצעד אחד לא החזיקה — משתנה סביבה שם יכול להיות מוגדר לכל הקשר בנפרד, או לרשת האתרים כולה, ולכן "נמחק" אינו מצב שאפשר להסתמך עליו מבחוץ.

הדרישה האמיתית לא השתנתה: הדאטהסט ציבורי וכל הקריאות הן של תוכן מפורסם, כך שהטוקן אינו מקנה שום יכולת. לכן התלות היחידה שהוא יצר הייתה תלות בכך שמישהו יגדיר אותו נכון — או בכלל לא. הסרת הקריאה מהקוד מחזירה את השליטה לקוד, ומצמצמת את מספר הדברים שצריכים להיות נכונים כדי שהאתר יציג תוכן, מארבעה לשניים.

**מה נדחה:**
המשך מרדף אחרי ההגדרה בממשק נטליפיי — נדחה כי הוא מותיר את נקודת הכשל בידי תצורה חיצונית שאינה בבקרת גרסאות, ושחוזרת ומפתיעה בכל סביבה חדשה. שליחת הטוקן עם נפילה חזרה לאנונימי אחרי שגיאת הרשאה — נדחה מאותו נימוק שברשומה 28: הסתרת שגיאת תצורה היא הסיבה שהתקלה חמקה שבועיים.

**השלכה:**
הרשומה מתקנת את סעיף ההשלכה ברשומה 28. סגירת הדאטהסט בעתיד תדרוש שינוי קוד ולא רק הגדרת משתנה — וזה מכוון: מעבר לקריאה מאומתת הוא החלטה, לא תופעת לוואי של משתנה סביבה שהופיע. הערך של `SANITY_API_READ_TOKEN` בנטליפיי אינו מזיק עוד, ואפשר למחוק אותו בנוחות ולא בדחיפות.

**מי החליט:** קלוד, אחרי שהמחיקה שכפיר ביצע לא שינתה את הפלט

**מקור:** פלט `‎/api/health/sanity` אחרי המחיקה — `readTokenPresent: true` לצד אותה שגיאת הרשאה; `lib/sanity.ts`; רשומה 28

---

## 30 הקטלוג מרונדר מנתונים, ברכיב שאינו יודע דבר על המשק

**תאריך:** 2026-09-15
**משימה:** חיבור הקטלוג ל-Sanity

**מה הוחלט:**
נוסף רכיב `PackageGrid` שמקבל מערך מארזים ומרנדר אותם ככרטיסים. הוא אינו מכיל טקסט, כתובות תמונה או ידע כלשהו על משק שוסטרמן — כל מה שהוא מציג מגיע ב-props. `CatalogSection` מרכיבה אותו מתחת לקולאז' הקיים, עם המארזים מ-Sanity ונפילה חזרה ל-`CONFIG` כשאין. שדה תיאור התמונה (`alt`) חובר מהסכמה ועד לתגית התמונה.

**למה:**
`CatalogSection` קיבלה prop בשם `packages` ולא השתמשה בו: המזהה הופיע פעם אחת בקובץ כולו, בפירוק הפרמטרים. מה שהוצג בפועל היה `PACKAGES_COLLAGE`, חמש כתובות Cloudinary קבועות בקוד. כלומר 13 המארזים שנשלפו מ-Sanity, הועברו לקומפוננטה ונזרקו — וכל עריכה של שם, תיאור, מחיר, תמונה או המתג "זמין" לא הגיעה לאף מבקר.

זהו אותו כשל שהחלטה 25 טיפלה בו בסטודיו (טופס שעריכתו אינה משנה דבר), אבל בגודל אחר לגמרי: לא שדה בודד אלא סוג התוכן המרכזי של האתר. מסירת הממשק לעורכת במצב הזה הייתה מוסרת לה לוח מחוונים מנותק.

הקולאז' נשמר. הוא עיצוב קיים שנבנה מחוץ למשימה הזו, והרשת נוספת מתחתיו במקום להחליף אותו.

הרכיב נכתב גנרי מפני שהממשק מיועד לשמש גם אתרים אחרים. תוכן קבוע בתוך רכיב תצוגה הוא בדיוק מה שמונע ממנו לעבור פרויקט.

**מה נדחה:**
החלפת הקולאז' ברשת — נדחה כמחיקת עיצוב קיים שלא נדרשה. הסתרת עורך המארזים מהסטודיו לפי העיקרון של החלטה 25 — נדחה: היא עקבית, אבל מותירה מערכת ניהול תוכן שמנהלת כותרות ופרטי קשר בלבד, ומרוקנת את הפרויקט.

**השלכה:**
כשהחיבור ל-Sanity עובד מוצגים 13 מארזים; בנפילה חזרה מוצגים ארבעת מארזי `CONFIG.packages` בלבד. הפרדת הקטגוריות ("מקו הגבול" מול מארזי המשק) אינה מיוצגת ברשת — כל המארזים מוצגים ברצף אחד. טרם הוכרע אם נדרשת הפרדה.

השדות שעדיין אינם מגיעים לאתר: הטקסט של "אודות" (`AboutSection` קוראת `CONFIG.about.story`; רק הכותרת מגיעה מ-Sanity), והמארזים בצ'אטבוט (`lib/botEngine.ts` קורא `CONFIG.packages`). שניהם פתוחים.

**מי החליט:** כפיר — "ממשק שישמש גם אתרים אחרים, יציב וגנרי"; בחירת המסלול האדריכלי נגזרה מכך

**מקור:** `components/sections/CatalogSection.tsx` לפני השינוי — `packagesProp` מופיע פעם אחת בקובץ; `lib/botEngine.ts:134`; `AboutSection.tsx:150,185,190,197`; ירידת אזהרות ESLint מ-7 ל-5 אחרי החיבור, כי האזהרה על ה-prop הלא מנוצל נעלמה

---

## 31 אזור "אודות" מקבל שדה לכל מקום בעיצוב, והצ'אטבוט מקבל את התוכן מבחוץ

**תאריך:** 2026-09-15
**משימה:** סגירת שני החיבורים שנותרו

**מה הוחלט:**
`about` בסכמה פוצל לשישה שדות בעלי שם: כותרת, פסקת פתיחה, ציטוט מודגש, כותרת ביניים, פסקת סיום, וציטוט התיבה הכהה. שדה `body` נשאר, והוגדר מחדש במפורש כטקסט שהצ'אטבוט עונה בו. `AboutSection` קוראת כל אחד מהם עם נפילה חזרה ל-`CONFIG` שדה-שדה.

`getBotResponse` קיבלה פרמטר שלישי אופציונלי, `content`, הנושא מארזים וטקסט אודות. ארבע ההפניות ל-`CONFIG.packages` וההפניה ל-`CONFIG.about` בתוכה הוחלפו בערכים שמגיעים ממנו. `RickyBot` מקבלת `content` ומעבירה הלאה; `app/page.tsx` מזרימה את מה שנשלף מ-Sanity.

חמשת השדות החדשים הוזרמו לדאטהסט עם הטקסט שמוצג היום באתר, ופורסמו.

**למה:**
אזור "אודות" אינו בלוק טקסט אחד אלא ארבעה תפקידים שונים בעיצוב: פסקה, ציטוט נטוי עם קו בצד, כותרת ביניים, ופסקה נוספת. `CONFIG.about.story` ייצג אותם כמערך, והרכיב זיהה את הציטוט לפי כך שהמחרוזת מתחילה בגרש. מיפוי שדה טקסט יחיד מהסטודיו על המבנה הזה היה מחייב חוק פיצול שהעורכת אינה רואה — שורה ריקה, מיקום במערך, או גרש פותח. כל אחד מהם הופך את השאלה "איפה הטקסט שכתבתי יופיע" לניחוש.

שדה לכל מקום פותר את זה ישירות: שם השדה אומר לאן הוא הולך, והתווית בסטודיו מתארת את המיקום ולא את התוכן.

ההזרמה נדרשה מאותה סיבה בדיוק. שדה חדש וריק לצד אתר שמציג טקסט מ-`CONFIG` הוא המלכודת שחזרה בפרויקט הזה שלוש פעמים: מה שרואים בממשק אינו מה שקורה באתר. אחרי ההזרמה, מה שריקי רואה בסטודיו הוא מה שמוצג.

בצ'אטבוט ההחלטה קטנה יותר: הזרקת תוכן במקום קריאה ישירה מ-`CONFIG`. ברירת המחדל נשמרה, כך שקריאה בלי הפרמטר עדיין עובדת והצ'אט אינו נשבר כשאין חיבור.

**מה נדחה:**
פיצול `body` לפסקאות לפי שורה ריקה — נדחה כחוק סמוי. מערך פסקאות בסכמה — נדחה: הוא מפורש יותר מפיצול, אבל העורכת עדיין לא יודעת שהפריט השלישי הופך לכותרת. השארת השדות החדשים ריקים והסתמכות על ה-fallback — נדחה מהסיבה שברשומה.

**השלכה:**
הקו בצד הציטוט היה `#BC6C25`, גוון ענבר, בניגוד לאילוץ המותג. הוא הוחלף לבורדו במסגרת שכתוב הבלוק. זהו שינוי ויזואלי קטן באתר קיים, והוא נעשה רק משום שהקוד הזה נכתב מחדש ממילא.

`CONFIG.about.pullQuotes[0]` עדיין בשימוש בטקסט השיתוף של `ShareButton`. זהו מטא-דאטה ולא תוכן מוצג, ולכן לא חובר.

**מי החליט:** כפיר — "סגור את שני החיבורים החסרים"; אופן המיפוי נגזר מהדרישה לממשק גנרי ויציב

**מקור:** `AboutSection.tsx` לפני השינוי — `CONFIG.about.story` בארבעה מקומות וזיהוי ציטוט לפי `para.startsWith('"')`; `lib/botEngine.ts:122,134,312,320,322`; שאילתת אימות מול הדאטהסט אחרי הפרסום — שישה שדות קיימים, `intro` 395 תווים ו-`story` 439, זהים למקור ב-`CONFIG`

---

## 32 תיאור תמונה הפך לחובה — אחרי שחובר, לא לפניו

**תאריך:** 2026-09-15
**משימה:** אכיפה עצמית של הממשק

**מה הוחלט:**
`alt` של התמונה הראשית במארז הפך לשדה חובה: אי אפשר לפרסם מארז שיש בו תמונה בלי תיאור. הוולידציה יושבת ב-`imageAltValidation` ב-`constants.ts` ומשותפת לכל סכמה. היא נדרשת רק כשהועלתה תמונה בפועל — שדה תמונה ריק אינו חוסם שמירה. מגבלת האורך נשארה אזהרה.

`alt` של הגלריה **לא** הפך לחובה, כי `gallery` מסומן `hidden` ואינו מרונדר.

**למה:**
בהודעה קודמת המלצתי להפוך את `alt` לחסימה, ובדיקה הראתה שההמלצה הייתה שגויה: באותו רגע `alt` לא נבחר ב-GROQ, לא הופיע ב-`Package`, והכרטיס ייצר טקסט חלופי משלו. חסימה הייתה מכריחה את העורכת למלא שדה שאינו משנה דבר — בדיוק הכשל שהחלטה 25 נועדה למנוע.

אחרי שהשדה חובר מקצה לקצה (רשומה 30), החסימה הפכה למשמעותית: היא מונעת תמונה בלי תיאור נגישות באתר שיש לו הצהרת נגישות, בלי שאיש יצטרך לבדוק. זו ההגדרה המעשית של ממשק שאינו דורש פיקוח.

הסדר הזה הוא הכלל, לא המקרה: **לא לאכוף שדה לפני שהוא מגיע לעמוד.** אכיפה על שדה מנותק מוסיפה חיכוך לעורכת ואפס ערך למבקר.

**מה נדחה:**
חסימה גם על `gallery.alt` — נדחתה מאותו נימוק שהוביל לדחיית החסימה המקורית.

**השלכה:**
נמצא מופע שלישי של אותה משפחת כשל: `reviewUrl` ב-`SiteFooter` חישב קישור "כתבו ביקורת בגוגל" שלא רונדר בשום מקום, ו-`CONFIG.googlePlaceId` עדיין מכיל את מחרוזת ההצבה `YOUR_GOOGLE_PLACE_ID`. הפיצ'ר מעולם לא הושלם. המשתנה הוסר; **הפיצ'ר עצמו לא נוסף**, כי הוספת בקשת ביקורת מלקוחות היא החלטה מוצרית ולא חיווט. כנ"ל `PremiumPlaceholder` ב-`CatalogSection` — רכיב שהוגדר ולא הורכב מעולם, שהוסר.

**מי החליט:** קלוד, במסגרת "תמשיך"; ההיפוך מההמלצה הקודמת נבע מבדיקת הקוד

**מקור:** `lib/sanity.ts` ו-`lib/types.ts` לפני רשומה 30 — `alt` לא נבחר ולא נשמר; `CatalogSection.tsx:137` ייצר טקסט חלופי מתבנית; `SiteFooter.tsx:34`; `lib/config.ts:9`

---

## 33 שתי בדיקות אוטומטיות שומרות על הקשר בין הסטודיו לאתר

**תאריך:** 2026-09-15
**משימה:** אכיפה עצמית של הממשק

**מה הוחלט:**
נוספו שתי בדיקות שרצות ב-CI על כל דחיפה, לצד `tsc` ו-`build`:

`scripts/check-cms-coverage.mjs` — מהדר את הסכמות עם ה-`tsc` שכבר בפרויקט, טוען אותן כאובייקטים אמיתיים, מחלץ את ההיטלים מה-GROQ שב-`lib/sanity.ts`, ונכשל כששדה שניתן לעריכה אינו נשלף על ידי אף שאילתה. שדה עם `hidden: true` או סוג שרשום ב-`UNRENDERED_TYPES` נחשב הצהרה מכוונת ועובר.

`scripts/check-lint-baseline.mjs` — נכשל כשמספר שגיאות ה-ESLint גדל מעל 21. במקביל, `@typescript-eslint/no-unused-vars` הועלה מאזהרה לשגיאה.

`UNRENDERED_TYPES` ו-`SINGLETONS` הועברו ל-`sanity/studio-policy.ts`, כדי ש-`sanity.config.ts` והבדיקה יקראו את אותה הצהרה.

**למה:**
שלושת הכשלים שנמצאו בפרויקט — הקטלוג שהתעלם מ-prop, אזור אודות שקרא מ-`CONFIG`, וקישור הביקורת בפוטר — חולקים תכונה אחת: **הם נראים תקינים.** טופס שעריכתו אינה משנה דבר מתנהג בדיוק כמו טופס שעובד. אין רגע שבו מישהו רואה שגיאה. לכן ביקורת אנושית אינה ההגנה הנכונה; צריך מכונה שתבחין.

שתי הבדיקות מכסות שני קטעים שונים של אותו מסלול, ואף אחת אינה מספיקה לבדה. בדיקת הכיסוי תופסת "שדה שאיש אינו שולף". `no-unused-vars` תופסת "נתון שנשלף ואז נזרק" — וזה בדיוק מה שקרה בקטלוג: ESLint דיווח על `packagesProp` כלא בשימוש לאורך כל התקופה, כאזהרה שאיש לא חסם עליה.

בסיס של 21 ולא אפס: מתוך השגיאות הקיימות, שלוש הן ממצאי React אמיתיים שתיקונם נושא סיכון התנהגותי, ו-13 הן `any` בקבצי סכמה. דרישת אפס היום הייתה מובילה לתיקון חפוז או לכיבוי השער. שער כבוי הוא איך שהבאג הזה שרד שבועיים.

**מה נדחה:**
פענוח הסכמות מטקסט המקור — נדחה כשביר; כל שינוי עיצוב בקובץ היה מזיז אותו. `npm run lint` כשער מלא — נדחה, ראו למעלה. שער שתופס רק `no-unused-vars` — נדחה לטובת בסיס כולל, שתופס גם סוגי שגיאות חדשים.

**השלכה:**
בדיקת הכיסוי אינה תופסת נתון שנשלף, מועבר לקומפוננטה, ונזרק בתוכה. זו המגבלה שמתועדת בראש הקובץ עצמו. `no-unused-vars` מכסה את המקרה כשהערך אינו בשימוש כלל, אך לא כשהוא בשימוש חלקי.

מנתח ה-GROQ מניח את צורת השאילתות שב-`lib/sanity.ts` היום. שאילתה שאינו מצליח לקרוא מדווחת במפורש ולא מדולגת בשתיקה.

השער תפס את הקובץ שבו הוא עצמו נכתב: ייבוא לא בשימוש ב-`check-cms-coverage.mjs`, בהרצה הראשונה.

**מי החליט:** כפיר — "ממשק יציב וגנרי עם יכולות"; זו היכולת שמעבירה את הלקח הזה לאתר הבא

**מקור:** הרצה עם `price` מוסר מההיטל — הבדיקה נכשלה עם `package.price` ויצאה בקוד 1, וחזרה לירוק אחרי שחזור; `npm run check` עובר; פירוט 21 השגיאות מתועד ב-`scripts/check-lint-baseline.mjs`

---

## 34 בסיס שגיאות הליטינג ירד מ-21 לשתיים, והשתיים שנשארו מתועדות כהחלטה

**תאריך:** 2026-09-17
**משימה:** צמצום החוב שנחשף בשער הליטינג

**מה הוחלט:**
תשע-עשרה מ-21 השגיאות תוקנו: 13 `any` הוחלפו בטיפוסים אמיתיים (טיפוס `Rule` המשותף בשלוש סכמות, וטיפוס `SanityImage` חדש ב-`lib/sanity.ts`), שלושה גרשים ב-JSX הוברחו, `@ts-ignore` הוסר לאחר שהתברר כמיותר, ממשק ריק הפך לכינוי טיפוס, והשמה ל-ref בזמן רינדור הועברה לאפקט. `BASELINE` ירד ל-2.

שתי השגיאות שנותרו — `set-state-in-effect` ב-`CookieBanner` וב-`AccessibilityWidget` — **נשארות גלויות במספר הבסיס ולא מושתקות ב-eslint-disable**.

**למה:**
שתיהן אותו דפוס: קריאת `localStorage` בתוך אפקט ואז `setState`. זו הצורה הנכונה לבעיה הזו ולא פספוס. `localStorage` אינו קיים ברינדור בצד שרת, ולכן אי אפשר לקרוא את הערך באתחול עצל של `useState` בלי שהשרת והדפדפן יחלקו על הציור הראשון. הכלל צודק שזה עולה רינדור נוסף; כאן זה המחיר של לא לשלוח אי-התאמת hydration.

הבחירה להשאיר אותן גלויות ולא להשתיק: `eslint-disable` מסתיר את השיקול בתוך שורה שאיש לא קורא. מספר בסיס עם הסבר בקובץ הבדיקה שומר את הפשרה במקום שבו מסתכלים עליה.

ההשמה ל-ref ב-`RickyBot` דווקא כן תוקנה, כי היא הפרה אמיתית: שינוי ref בזמן רינדור שובר את הערובות של React ברינדור מקבילי. הקורא היחיד הוא `handleUserInput`, שרץ מאירוע משתמש — כלומר אחרי שאפקטים נשטפו — ולכן ההעברה לאפקט אינה משנה התנהגות.

**מה נדחה:**
`useSyncExternalStore` עם snapshot לשרת — הפתרון הנכון לשתי השגיאות שנותרו, ונדחה כרגע: הוא דורש פונקציית subscribe ל-`storage` ומכונה נוספת, בשביל באנר עוגיות שמרונדר פעם אחת. שווה לחזור אליו כשאחד מהרכיבים יגדל.

השתקה ב-`eslint-disable` — נדחתה כמתואר.

**השלכה:**
השער נעשה הדוק בהרבה: מעכשיו כמעט כל שגיאה חדשה תיחסם. שינוי הגרשים הוא `&apos;` ולא התו העברי `׳`, בכוונה — `&apos;` מרנדר בדיוק את אותו תו ASCII, כך שאין שינוי ויזואלי ואין סיכון להתאמות המחרוזות של הצ'אטבוט, שמשווה `"ליצ'י"` עם גרש ASCII.

**מי החליט:** קלוד, במסגרת "תמשיך"

**מקור:** פלט `eslint -f json` לפני (21 שגיאות) ואחרי (2); `npm run check` ו-`next build` עוברים; `components/RickyBot.tsx:53` לפני השינוי — `stateRef.current = state` בגוף הרכיב, ו-`:108` הקורא היחיד

---

## לבירור מול כפיר

הפריטים הבאים אינם החלטות סגורות. הם נרשמו כאן ולא כרשומות כדי לא לתעד הכרעה שלא התקבלה.

**א. חוסר התאמה בשמות משתני הסביבה.**
שכבת הקריאה הקיימת בקוד קוראת לשמות ללא תחילית ציבורית, בעוד קובץ הדוגמה שנמסר במשימה 1 מתעד שמות עם תחילית ציבורית. כלומר הסטודיו והאתר עשויים להצביע על מקורות שונים. שאלתי על כך לפני משימה 1 והשאלה נסגרה בלי מענה. הקובץ הקיים לא נגע. **דורש הכרעה לפני משימה 4.** נסגר ברשומה 18.

**ב. חובת טקסט חלופי לתמונות.**
המפרט קבע שכל תמונה מחייבת טקסט חלופי, וההנחיה למשימה 2 קבעה שאין לחסום עריכה. שני הכללים סותרים. יושם כאזהרה, כלומר לא חוסם. הועלה לאישור ולא נענה.

**ג. שפת ממשק הסטודיו.**
אין חבילת לוקאליזציה עברית רשמית לסטודיו. שמות המסמכים והשדות בעברית, אך כפתורי הסטודיו עצמו יישארו באנגלית אלא אם תיבנה חבילת תרגום ייעודית. דווח ולא נענה.
