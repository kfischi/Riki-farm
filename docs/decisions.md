# יומן החלטות — החווה של ריקי

מסמך זה מתעד החלטות ארכיטקטוניות ואת הנימוק להן.
נכתב תוך כדי העבודה. מהווה בסיס למפרט טכני לשכפול.

---

## 01 Sanity כ-backend התוכן

**תאריך:** 2026-08-18
**משימה:** החלטת ארכיטקטורה (לפני משימה 0)

**מה הוחלט:**
ניהול התוכן ייבנה על Sanity, ולא על פתרון עצמאי.

**למה:**
טיפול בתמונות מובנה (העלאה, crop, hotspot, CDN) — הרכיב הכבד ביותר לבנייה עצמית. אין צורך לבנות הרשאות, גרסאות ו-audit log מאפס. עריכה לא דורשת deploy מחדש, ויש היסטוריה שמאפשרת לשחזר טקסט שנמחק בטעות.

**מה נדחה:**
JSON בריפו — כל שינוי דורש commit, לא מתאים למשתמשת לא טכנית. בניית DB ופאנל admin מאפס — חודש עבודה על משהו שקיים מהמדף.

**מי החליט:** כפיר

**מקור:** ההנחיה בשיחה, סעיף "החלטת ארכיטקטורה"

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 02 סטודיו מוטמע באפליקציית Next, ולא סטודיו מתארח

**תאריך:** 2026-08-18
**משימה:** 1

**מה הוחלט:**
הסטודיו רץ בתוך אותו פרויקט Next.js בנתיב `/studio`, ולא כאפליקציה נפרדת שמתארחת אצל Sanity.

**למה:**
הנימוק לא תועד בזמן ההחלטה.

**מה נדחה:**
לא נשקלה אלטרנטיבה.

**מי החליט:** כפיר

**מקור:** ההנחיה במשימה 1, סעיף 2

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 03 ייבוא קונפיג הסטודיו מאחורי גבול `'use client'`

**תאריך:** 2026-08-18
**משימה:** 1

**מה הוחלט:**
`sanity.config.ts` מיובא מקומפוננטת לקוח ייעודית ולא ישירות מ-`page.tsx`.

**למה:**
חבילת `sanity` מייבאת את ה-default של `swr`. כשהקונפיג נמצא בגרף ה-Server Component חל תנאי הייצוא `react-server`, ובבילד הזה של `swr` אין default export — הראוט החזיר 500.

**מה נדחה:**
עקיפה ב-`next.config.ts` (alias או `serverExternalPackages`) — נדחתה כי היא מסתירה את הסיבה ומשפיעה על כל הפרויקט, במקום לתחום את הבעיה למקום שבו היא נוצרת.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** קומיט `fd56680`, הקובץ `app/studio/[[...tool]]/Studio.tsx`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 04 דריסות CSS מתוחמות לסטודיו, במקום layout שורש נפרד

**תאריך:** 2026-08-18
**משימה:** 1

**מה הוחלט:**
ה-layout של `/studio` דורס כללים גלובליים ומסתיר ווידג'טים של האתר, במקום להוציא את האתר ל-route group עם layout שורש משלו.

**למה:**
ה-layout הראשי מזריק לכל ראוט את טבעת הפוקוס של האתר (שמבוססת על צבע שאסור לפי אילוץ המותג), ומרנדר את ווידג'ט הנגישות ובאנר העוגיות כילדים ישירים של `body`. שניהם לא שייכים למסך ניהול.

**מה נדחה:**
העברת כל עמודי האתר ל-route group בשם `(site)` — נדחתה כי היא נוגעת בעשרות קבצים שאינם קשורים למשימה.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** קומיט `fd56680`, הקובץ `app/studio/[[...tool]]/layout.tsx`

**האם ניתן לשכפול ללקוח אחר:** כן בשינוי — תלוי במה ה-layout הראשי של הלקוח מזריק

---

## 05 חריג ב-`.gitignore` לקובץ הדוגמה של משתני הסביבה

**תאריך:** 2026-08-18
**משימה:** 1

**מה הוחלט:**
נוספה שורת שלילה ב-`.gitignore` כדי שקובץ הדוגמה ייכנס לגיט, בעוד קבצי הסביבה האמיתיים נשארים חסומים.

**למה:**
הכלל הקיים `.env*` תפס גם את קובץ הדוגמה, שהוא תוצר מחייב של המשימה. בלי החריג אי אפשר היה למסור אותו.

**מה נדחה:**
לתת לקובץ שם שלא מתחיל בנקודה כדי לעקוף את הכלל — נדחתה כי היא שוברת את המוסכמה המקובלת ומקשה על מפתח חדש למצוא את הקובץ.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** קומיט `fd56680`, הקובץ `.gitignore`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 06 הרחבת הסכמות הקיימות במקום יצירת סכמות חדשות

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
הסכמות הקיימות בריפו הורחבו במקומן, ולא נוצרו לצידן שלוש סכמות חדשות כפי שהמפרט ביקש.

**למה:**
שתיים משלוש הסכמות במפרט חפפו כמעט לחלוטין לסכמות שכבר היו בריפו. יצירתן במקביל הייתה משאירה את העורכת עם שני סוגי "מארז" בלי דרך לדעת איזה מהם האתר קורא.

**מה נדחה:**
יצירת שלוש הסכמות החדשות לצד הקיימות — נדחתה בגלל הכפילות. מחיקת הקיימות ובנייה מחדש — נדחתה כי היא שוברת את שכבת הקריאה הקיימת בקוד.

**מי החליט:** כפיר, באישור המלצה של Claude Code

**מקור:** קומיט `cdec538`, וההנחיה "קדימה — הרחב את הסכמות הקיימות. אל תיצור סכמות חדשות שכפולות."

**האם ניתן לשכפול ללקוח אחר:** כן בשינוי — רלוונטי רק כשקיימות סכמות קודמות בריפו

---

## 07 `mediaBlock` כסכמה חדשה יחידה

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
מתוך שלוש הסכמות שהמפרט ביקש, רק `mediaBlock` נוצרה כסכמה חדשה.

**למה:**
נבדקו כל חמש הסכמות הקיימות ולא נמצא בהן שדה או ישות שמתאימים לבאנרים ולתמונות אתר. זו הסכמה היחידה שאינה כפילות.

**מה נדחה:**
הרחבה של אחת הסכמות הקיימות כדי לארח בה גם תמונות אתר — נדחתה כי מדובר בישויות שונות, והערבוב היה מבלבל את העורכת.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** קומיט `cdec538`, הקובץ `sanity/schemas/mediaBlock.ts`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 08 צבע הבאנר — טיפוס טקסט עם רשימה סגורה

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
שדה צבע הבאנר נשאר מטיפוס טקסט, אך קיבל רשימת ערכים סגורה מפלטת המותג בלבד.

**למה:**
הכלל שנקבע למשימה אסר לשנות טיפוס של שדה קיים. במקביל, ההגדרה הקודמת של השדה הציעה לעורכת במפורש ערך שאסור לפי אילוץ המותג — כלומר הסטודיו עצמו הכווין אותה לצבע פסול. רשימה סגורה פותרת את שתי המגבלות יחד.

**מה נדחה:**
שינוי הטיפוס לטיפוס צבע ייעודי, ומחיקת השדה — שניהם נדחו כי הכלל אסר לשנות טיפוס או למחוק שדה קיים.

**מי החליט:** כפיר

**מקור:** ההנחיה "banner.color — שנה description. בנוסף: הוסף options.list עם ערכים סגורים מפלטת המותג בלבד", והקובץ `sanity/schemas/siteSettings.ts`

**האם ניתן לשכפול ללקוח אחר:** כן בשינוי — הפלטה עצמה ספציפית ללקוח

---

## 09 שדות המחיר נשארים טקסט

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
שדה המחיר הקיים נשאר טקסט, ושדה המחיר החדש לכמות גדולה נוצר גם הוא כטקסט.

**למה:**
הכלל למשימה אסר לשנות טיפוס של שדה קיים, והשדה הקיים הוגדר כטקסט מלכתחילה כדי לאפשר ניסוחים כמו "לפי הצעת מחיר". השדה החדש הוגדר באותו טיפוס כדי שלא יהיו שני מחירים בהתנהגות שונה.

**מה נדחה:**
הגדרת שדות המחיר כמספרים, כפי שהמפרט רמז — נדחתה בגלל הכלל, ובגלל שניסוחים שאינם מספר הם דרישה עסקית אמיתית.

**מי החליט:** כפיר

**מקור:** ההנחיה "price — נשאר string. bulkPrice גם string. אשר.", והקובץ `sanity/schemas/package.ts`

**האם ניתן לשכפול ללקוח אחר:** כן בשינוי

---

## 10 קטגוריה כרשימה סגורה, בנפרד מהתגיות

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
נוסף שדה קטגוריה עם רשימת ערכים סגורה, והשדה הקיים לתגיות חופשיות נשאר כפי שהיה. רשימות הערכים רוכזו בקובץ קבועים אחד.

**למה:**
העורכת אינה טכנית. הקלדה חופשית של שם עונה מייצרת גרסאות שונות לאותו ערך, והאתר מפסיק לסנן נכון. ריכוז הרשימה במקום אחד הופך הוספת עונה לשינוי של שורה אחת.

**מה נדחה:**
שימוש חוזר בשדה התגיות החופשי לצורך קטגוריה — נדחה בגלל סכנת חוסר העקביות.

**מי החליט:** כפיר, באישור המלצה של Claude Code

**מקור:** ההנחיה "אפשרות (ב) — category חדש עם רשימה סגורה, tags נשאר חופשי", והקובץ `sanity/schemas/constants.ts`

**האם ניתן לשכפול ללקוח אחר:** כן — ערכי הרשימה משתנים בין לקוחות

---

## 11 ולידציה כאזהרה בלבד, לא כחסימה

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
כל מגבלות האורך שנוספו הוגדרו כאזהרה. אף מגבלה חדשה אינה חוסמת שמירה.

**למה:**
עדיף שהעורכת תשמור תוכן ארוך מדי ותראה הערה, מאשר שתיתקע ולא תוכל לשמור כלל. חסימה אצל משתמשת לא טכנית מובילה לנטישת הממשק.

**מה נדחה:**
ולידציה חוסמת — נדחתה מהסיבה שלמעלה. חובות ה-`required` שכבר היו בקוד לא שונו.

**מי החליט:** כפיר

**מקור:** ההנחיה "השתמש ב-warning ולא ב-error, כדי לא לחסום עריכה"

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 12 אכיפת singleton להגדרות האתר בשתי שכבות

**תאריך:** 2026-08-18
**משימה:** 2

**מה הוחלט:**
מסמך הגדרות האתר נאכף כמסמך יחיד: מזהה מסמך קבוע ופריט יחיד בסרגל, ובנוסף סינון מכל תפריטי "צור חדש".

**למה:**
מבנה סרגל בלבד אינו מספיק — כפתור ההוספה הגלובלי עדיין מאפשר ליצור עותק שני, והאתר היה קורא רק אחד מהם. שתי השכבות יחד סוגרות את הפרצה.

**מה נדחה:**
הסתרה באמצעות מבנה הסרגל בלבד — נדחתה כי אינה מונעת יצירה בפועל.

**מי החליט:** כפיר

**מקור:** ההנחיה "כלול את אכיפת ה-singleton של siteSettings... הסתר את כפתור 'צור חדש' עבורו", והקובץ `sanity.config.ts`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 13 סקריפט ההזרמה מוגן בהרצה יבשה כברירת מחדל

**תאריך:** 2026-08-18
**משימה:** 3

**מה הוחלט:**
הסקריפט רץ במצב יבש כברירת מחדל ואינו כותב דבר, אלא אם הועבר לו במפורש דגל כתיבה.

**למה:**
הרצה בשוגג של סקריפט הזרמה על דאטהסט חי היא נזק שקשה לתקן. ברירת מחדל שאינה כותבת מאפשרת לראות בדיוק מה יקרה לפני שמשהו קורה. בנוסף, כל מסמך נכתב עם מזהה קבוע ובפעולה שאינה דורסת, כך שהרצה חוזרת לא מוחקת עריכות של העורכת.

**מה נדחה:**
סקריפט שכותב מיד בהרצה — נדחה בגלל סיכון הדריסה. כתיבה דורסת — נדחתה כי היא הופכת כל הרצה חוזרת למחיקת עבודה.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** הקובץ `scripts/seed-sanity.ts`, הדגל `--commit`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 14 סרטונים עם קישור זמני לא הוזרמו

**תאריך:** 2026-08-18
**משימה:** 3

**מה הוחלט:**
שני הסרטונים שבקוד לא הועברו לניהול התוכן.

**למה:**
הקישור שלהם בקוד הוא ערך זמני ולא כתובת אמיתית, והשדה בסכמה מוגדר כחובה. הזרמה הייתה יוצרת שני מסמכים שנראים כתוכן תקין אך מובילים לשום מקום, והעורכת לא הייתה יודעת שהם ריקים.

**מה נדחה:**
הזרמת הערך הזמני כפי שהוא — נדחתה כי היא מזריקה נתון מזויף למערכת. המצאת קישור אמיתי — אסורה לפי כלל העבודה.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** `lib/config.ts`, מערך הסרטונים; והפלט של הסקריפט תחת "תוכן שלא הועבר"

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 15 הקטגוריה לא נגזרה מהתגיות הקיימות

**תאריך:** 2026-08-18
**משימה:** 3

**מה הוחלט:**
שדה הקטגוריה נשאר ריק בכל המארזים שהוזרמו, במקום להיגזר אוטומטית מהתגיות שכבר קיימות בקוד.

**למה:**
התגיות בקוד אינן מיפוי חד-ערכי לרשימה הסגורה שנקבעה במשימה 2 — למארז אחד יש כמה תגיות, וחלקן אינן מתאימות לאף ערך ברשימה. גזירה אוטומטית הייתה בחירה שלי ולא נתון שקיים בקוד.

**מה נדחה:**
גזירת הקטגוריה מהתגית הראשונה — נדחתה כי היא ניחוש שנראה כמו נתון.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** הכלל "אל תמציא" במשימה 3; הפלט של הסקריפט תחת "שדות לא מולאו"

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 16 רוב תוכן האתר אינו מכוסה על ידי הסכמות

**תאריך:** 2026-08-18
**משימה:** 3

**מה הוחלט:**
ארבעה-עשר גושי תוכן שקיימים בקוד לא הוזרמו, מפני שאין להם שדה באף סכמה. הם תועדו ולא נמחקו ולא נדחסו לשדה קיים.

**למה:**
המפרט המקורי הגדיר שלוש סכמות בלבד. שאלות ותשובות, המלצות, יתרונות, פסקאות הסיפור, הפרטים המשפטיים ומחירון הצ'אטבוט נשארים מחוץ להישג ידה של העורכת. זו מגבלת היקף אמיתית שנחשפה רק בשלב ההזרמה.

**מה נדחה:**
הרחבת הסכמות ביוזמתי כדי לכסות את הפער — נדחתה כי היא חורגת מהיקף המשימה ומחייבת החלטה של בעל המוצר. דחיסת התוכן לשדות טקסט קיימים — נדחתה כי היא הופכת תוכן מובנה לטקסט חופשי.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** הפלט של `scripts/seed-sanity.ts` תחת "תוכן שלא הועבר"

**האם ניתן לשכפול ללקוח אחר:** כן בשינוי — היקף הפער תלוי בכמות התוכן אצל הלקוח

---

## 17 התוכן הוזרם דרך ערוץ חלופי, בלי תמונות

**תאריך:** 2026-08-18
**משימה:** 3

**מה הוחלט:**
שלושים מסמכי טקסט הוזרמו ופורסמו דרך ממשק הניהול של Sanity במקום דרך הסקריפט, וארבעה בלוקי מדיה וכל התמונות לא הוזרמו כלל.

**למה:**
מדיניות הרשת של סביבת הריצה חוסמת גם את ממשק ה-API של Sanity וגם את שירות התמונות שבו יושבות תמונות האתר. הסקריפט עצמו תקין, אך לא יכול לרוץ מכאן. המטענים נוצרו מהסקריפט עצמו כדי שלא תהיה סטייה בין מה שאושר לבין מה שנכתב בפועל.

**מה נדחה:**
המתנה עד שיתאפשר להריץ את הסקריפט — נדחתה כי היא חוסמת את משימה 4 ללא צורך. הזרמת בלוקי מדיה בלי תמונה — נדחתה כי התמונה היא שדה חובה שם, והמסמך היה נוצר פגום.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** תשובת הפרוקסי 403 עבור שני היעדים; הדגל `--json` ב-`scripts/seed-sanity.ts`

**האם ניתן לשכפול ללקוח אחר:** לא — מגבלת סביבה, לא החלטת מוצר. בסביבה עם גישת רשת מריצים את הסקריפט כרגיל והתמונות עוברות איתו.

---

## 18 יישור שמות משתני הסביבה בין האתר לסטודיו

**תאריך:** 2026-08-18
**משימה:** 4

**מה הוחלט:**
שכבת הקריאה של האתר עברה לקרוא את מזהי הפרויקט והדאטהסט מאותם שמות שהסטודיו משתמש בהם, וטוקן הקריאה יושר לשם שמתועד בקובץ הדוגמה. סוגר את פריט א' ב"לבירור מול כפיר".

**למה:**
עד כה האתר קרא שמות ללא תחילית ציבורית והסטודיו קרא שמות עם תחילית, כך ששניהם יכלו להצביע על מקורות שונים בלי שאיש ישים לב. סטודיו מוטמע רץ בדפדפן ולכן מחויב לתחילית הציבורית; המזהים אינם סודות, והטוקן נשאר בצד השרת בלבד.

**מה נדחה:**
יישור לכיוון ההפוך, כלומר הסרת התחילית — נדחתה כי הסטודיו לא היה עולה בלעדיה.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** `lib/sanity.ts`; פריט א' ב"לבירור מול כפיר" ברשומות הקודמות

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 19 קריאה ישירה מ-Sanity ולא דרך ה-CDN שלו

**תאריך:** 2026-08-18
**משימה:** 4

**מה הוחלט:**
הלקוח מוגדר לא לעבור דרך ה-CDN של Sanity.

**למה:**
שכבת המטמון של האתר היא ISR של דקה בתוספת webhook לרענון מיידי. מטמון נוסף ובלתי נשלט מעל זה היה יכול לגרום לכך שפרסום ייקח יותר מהדקה שהובטחה, בלי שנוכל לדעת מתי.

**מה נדחה:**
השארת ה-CDN דלוק — נדחתה בגלל אי-הוודאות בזמן ההתעדכנות. המחיר: יותר קריאות ל-API, אך ISR מגביל אותן לקריאה אחת לדקה לכל היותר.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** `lib/sanity.ts`, ההגדרה `useCdn`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 20 ראוט הרענון סגור כברירת מחדל ומרענן נתיב אחד

**תאריך:** 2026-08-18
**משימה:** 4

**מה הוחלט:**
ראוט הרענון דורש סוד משותף בכותרת, מחזיר שגיאה כשהסוד אינו מוגדר כלל, ומרענן את עמוד הבית בלבד.

**למה:**
בלי בדיקת סוד כל אחד ברשת יכול היה להפעיל רענון חוזר ולהעמיס על האתר ועל Sanity. סביבה שבה הסוד לא הוגדר היא סביבה שבה הראוט פתוח, ולכן הוא נסגר מעצמו במקום לעבוד ללא הגנה. ההשוואה נעשית בזמן קבוע כדי לא לדלוף מידע על הסוד.

**מה נדחה:**
רענון לפי סוג המסמך שהשתנה — נדחה כי כל התוכן הקיים מוזן לעמוד הבית בלבד, וגרנולריות מדומה הייתה מטעה. ראוט ללא הגנה — נדחה מטעמי אבטחה.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** `app/api/revalidate/route.ts`

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 21 שרשרת נפילה לתמונות מארז, והסרת צבע אסור מברירות המחדל

**תאריך:** 2026-08-18
**משימה:** 4

**מה הוחלט:**
מארז ללא תמונה בניהול התוכן מציג את התמונה הסטטית הקיימת באתר לפי המזהה שלו, ורק אם גם היא חסרה מוצגת תמונת ברירת מחדל בצבעי המותג.

**למה:**
ההזרמה הכניסה שלושה-עשר מארזים בלי תמונה. ברירת המחדל שהייתה בקוד צבועה בענבר, ולכן חיבור האתר לניהול התוכן היה מחליף תמונות אמיתיות בשלושה-עשר ריבועים בצבע שאסור לפי אילוץ המותג. שרשרת הנפילה שומרת על התמונות שכבר קיימות ומסירה את הצבע האסור.

**מה נדחה:**
השארת ברירת המחדל כפי שהייתה — נדחתה כי היא מפרה את אילוץ המותג. הצגת מארז ללא תמונה כלל — נדחתה כי היא שוברת את פריסת הקטלוג.

**מי החליט:** Claude Code בהמלצה שאושרה

**מקור:** `lib/sanity.ts`, הפונקציה שממפה מארזים

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 22 מזהי Sanity הציבוריים בקובץ התצורה, הסודות בממשק Netlify

**תאריך:** 2026-08-19
**משימה:** 4

**מה הוחלט:**
מזהה הפרויקט ושם הדאטהסט נכתבו לתוך `netlify.toml`. טוקן הקריאה וסוד הרענון נשארים להגדרה ידנית בממשק Netlify ומסומנים כסודות.

**למה:**
שני המזהים נושאים את הקידומת שגורמת ל-Next להטמיע אותם בחבילת הדפדפן בזמן הבנייה, מפני שהסטודיו רץ בדפדפן וזקוק להם. כלומר הם ממילא גלויים לכל מי שפותח את האתר, וניהולם כסודות יוצר עבודה ידנית בלי להגן על דבר. אומת בפועל: המזהה מופיע גם בחבילת השרת וגם בחבילת הלקוח שנבנו. שני הסודות האמיתיים לא עברו לקובץ, כי קובץ התצורה נכנס לגיט.

**מה נדחה:**
הגדרת ארבעת המשתנים בממשק Netlify — נדחתה כי היא מטילה על בעל האתר ארבע פעולות ידניות במקום שתיים, ושתיים מהן חסרות ערך אבטחתי. כתיבת הטוקן לקובץ התצורה — נדחתה כי הקובץ מנוהל בגיט.

**מי החליט:** כפיר, בתשובה "כן תוסיף"

**מקור:** `netlify.toml`, וקובץ ההנחיות של Next תחת `node_modules/next/dist/docs`, המסמך על משתני סביבה

**האם ניתן לשכפול ללקוח אחר:** כן

---

## 23 הרשאות מקור לדומיין הפרודקשן בפרויקט Sanity

**תאריך:** 2026-08-19
**משימה:** 4

**מה הוחלט:**
נוספו שתי הרשאות מקור לפרויקט התוכן — הדומיין הרשמי של האתר, ודומיין האירוח. שתיהן עם הרשאת בקשות מזוהות.

**למה:**
לפני השינוי היו מוגדרות שתי הרשאות בלבד, שתיהן לכתובות פיתוח מקומיות. בלעדיהן מסך הניהול בפרודקשן היה נטען אך נכשל בפנייה לשרת התוכן, ומציג שגיאת רשת במקום מסך התחברות. הדומיין הרשמי נלקח מהגדרת הכתובת הקנונית בקוד האתר, ולא מהשערה.

**מה נדחה:**
הרשאת תו כללי לכל תת-הדומיינים של שירות האירוח — נדחתה כי היא פותחת בקשות מזוהות מכל אתר המתארח באותו שירות. המשמעות היא שמסך הניהול לא ייטען בתצוגות מקדימות של בקשות מיזוג, אלא אם תתווסף הרשאה ייעודית.

**מי החליט:** כפיר, בתשובה "כן תוסיף"

**מקור:** `app/layout.tsx`, הגדרת הכתובת הקנונית; ורשימת הרשאות המקור בפרויקט לפני השינוי ואחריו

**האם ניתן לשכפול ללקוח אחר:** כן

---

## לבירור מול כפיר

הפריטים הבאים אינם החלטות סגורות. הם נרשמו כאן ולא כרשומות כדי לא לתעד הכרעה שלא התקבלה.

**א. חוסר התאמה בשמות משתני הסביבה.**
שכבת הקריאה הקיימת בקוד קוראת לשמות ללא תחילית ציבורית, בעוד קובץ הדוגמה שנמסר במשימה 1 מתעד שמות עם תחילית ציבורית. כלומר הסטודיו והאתר עשויים להצביע על מקורות שונים. שאלתי על כך לפני משימה 1 והשאלה נסגרה בלי מענה. הקובץ הקיים לא נגע. **דורש הכרעה לפני משימה 4.** נסגר ברשומה 18.

**ב. חובת טקסט חלופי לתמונות.**
המפרט קבע שכל תמונה מחייבת טקסט חלופי, וההנחיה למשימה 2 קבעה שאין לחסום עריכה. שני הכללים סותרים. יושם כאזהרה, כלומר לא חוסם. הועלה לאישור ולא נענה.

**ג. שפת ממשק הסטודיו.**
אין חבילת לוקאליזציה עברית רשמית לסטודיו. שמות המסמכים והשדות בעברית, אך כפתורי הסטודיו עצמו יישארו באנגלית אלא אם תיבנה חבילת תרגום ייעודית. דווח ולא נענה.
